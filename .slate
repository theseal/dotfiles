# Configs.
config defaultToCurrentScreen true
config checkDefaultsOnLoad true
config focusCheckWidthMax 3000
config windowHintsShowIcons true
config windowHintsIgnoreHiddenWindows false
config windowHintsSpread true

# Monitor Aliases.
alias external 1920x1080
alias laptop 1366x768

# Resolution aliases.
default external resolutions:${external}
default laptop   resolutions:${laptop}
default 2screen  resolutions:${external};${laptop}

# Misc aliases.
alias showHintsLeftHand hint AOEUIYQJKX
alias showNormalHint hint AOEUIDHTNSYXFBPKGMCW
bind 0:ctrl relaunch

# Abstract positions.
alias full          move screenOriginX;screenOriginY                  screenSizeX;screenSizeY
alias lefthalf      move screenOriginX;screenOriginY                  screenSizeX/2;screenSizeY
alias righthalf     move screenOriginX+screenSizeX/2;screenOriginY    screenSizeX/2;screenSizeY
alias rightmost     move screenOriginX+screenSizeX*0.4;screenOriginY  screenSizeX*0.6;screenSizeY
alias rightless     move screenOriginX+screenSizeX*0.6;screenOriginY  screenSizeX*0.4;screenSizeY
alias leftmost      move screenOriginX;screenOriginY                  screenSizeX*0.6;screenSizeY
alias leftless      move screenOriginX;screenOriginY                  screenSizeX*0.4;screenSizeY
alias up            move screenOriginX;screenOriginY                  screenSizeX;screenSizeY/2
alias down          move screenOriginX;screenOriginY+screenSizeY/2    screenSizeX;screenSizeY/2
alias downleft      move screenOriginX;screenOriginY+screenSizeY/2  screenSizeX/2;screenSizeY/2
alias upleft        move screenOriginX;screenOriginY  screenSizeX/2;screenSizeY/2
alias upright	    move screenOriginX+screenSizeX/2;screenOriginY    screenSizeX/2;screenSizeY/2
alias downright     move screenOriginX+screenSizeX/2;screenOriginY+screenSizeY/2  screenSizeX/2;screenSizeY/2

# External monitor only.
layout external 'iTerm' ${righthalf} 
layout external 'Mail' ${righthalf} 
layout external 'Reeder' ${righthalf} 
layout external 'Firefox' ${lefthalf} 
layout external 'iPhoto' ${full}

# Two screens.
layout 2screen '1Password' ${full}
layout 2screen 'iTerm' ${righthalf} ${external}
layout 2screen 'Mail' ${full} ${laptop}
layout 2screen 'Reeder' ${righthalf} ${external}
layout 2screen 'Firefox' ${lefthalf} ${external} 
layout 2screen 'iPhoto' ${full} ${external}
layout 2screen '1Password' ${full} ${laptop}

# Laptop only.
layout laptop 'iTerm' ${full}
layout laptop 'Mail' ${full}
layout laptop 'Reeder' ${full}
layout laptop 'Firefox' ${full}
layout laptop 'iPhoto' ${full}
layout laptop '1Password' ${full}

# Key bindings.
bind right:ctrl;shift resize +10% +0
bind left:ctrl;shift resize -10% +0
bind down:ctrl;shift resize +0 +10%
bind up:ctrl;shift resize +0 -10%
bind return:ctrl ${full}
bind left:ctrl ${lefthalf}
bind right:ctrl ${righthalf}
bind up:ctrl ${up}
bind down:ctrl ${down}
bind left:ctrl;alt ${upleft}
bind down:ctrl;alt ${downleft}
bind right:ctrl;alt ${downright}
bind up:ctrl;alt ${upright}
